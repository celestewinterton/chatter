import { useSelector } from "react-redux"

const GroupsPage = () => {

    // const groups = useSelector(state => state.groups.all)

    return (
        <div className="app-body">
            <div className="groups-header">
                <h1 className="groups-title">GroupsPage</h1>
            </div>
        </div>
    )

}

export default GroupsPage
